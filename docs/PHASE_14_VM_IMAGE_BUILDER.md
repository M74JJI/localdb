# Phase 14 — VM Image Builder

## Added

- Packer scaffold
- preseed scaffold
- cloud-init scaffold
- first-boot info script
- OVA release checklist

## Recommended first real release

Start with a manually built golden Debian VM using the Phase 13 installer. Export that VM as OVA.

Once the manual OVA is validated, automate the build with Packer.
